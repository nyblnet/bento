# Copy to keystore.properties (gitignored) and fill in.
# Create a keystore with:
#   keytool -genkeypair -v -keystore release.keystore -alias bento \
#           -keyalg RSA -keysize 2048 -validity 10000
storeFile=release.keystore
storePassword=
keyAlias=bento
keyPassword=
